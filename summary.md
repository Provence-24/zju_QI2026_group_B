# 项目演进总结：量子表面码逻辑错误率对比研究

## 第一阶段：项目奠基（2026-04-28）

**提交：** `5a5fc67` Initial commit / `ed8d6ad` 修复安装脚本 / `aadfb05` 补充结果

**代码成果：**
- 搭建了完整的表面码研究框架，包含 15 个源文件、2481 行代码。
- 核心模块就位：`simulator.py`（stim 仿真引擎，475 行）、`circuit_builder.py`（电路构建）、`platforms.py`（三平台噪声参数定义）、`analysis/fitting.py` 和 `plotting.py`（数据分析与可视化）。
- 三个实验脚本：实验一（PL vs 物理错误率 p）、实验二（PL vs 码距 d）、实验三（三平台横向对比）。
- 使用 MWPM（最小权完美匹配）作为唯一解码器，通过 pymatching 实现。
- 首次运行实验并产出 8 个结果文件（JSON 数据 + PDF/PNG 图表）。

**问题与解决：**
- Linux/Mac 的 `setup.sh` 命令有误 → `ed8d6ad` 修复。
- `how_to_run.md` 有一处小错误 → `2f25282` 修复。

---

## 第二阶段：基线完成与文档化（2026-05-05）

**提交：** `8a9d212` 完成 baseline

**代码成果：**
- 基线实验全部完成，三平台的表面码对比有了可复现的初始结果。
- 补充了实验说明文档（`experiments/README.md`，226 行），详细记录了实验目的、参数设置和运行方式。
- 创建 TODO 文件，明确了后续方向：平台物理特性调研、解码器扩展、编译器改进。

**实验结论（builtin 模式，仅 stim 内置噪声模型）：**
- 三平台在 stim 内置电路下均表现出标准的表面码行为——PL 随 p 降低、随 d 增大而指数级下降。
- 各平台排位：离子阱 > 超导 > 中性原子（PL 从低到高）。离子阱因 idle 噪声极低（p_idle=10⁻⁶）而表现最优。
- 此阶段所有结论基于简化的聚合噪声模型，**不区分各平台的物理实现过程**（移动、输运、激发态退相干等）。

**问题与解决：**
- 此前实验缺少系统性文档，外部难以理解实验设计 → 补写了完整的实验说明。

---

## 第三阶段：解码器接口抽象（2026-05-10）

**提交：** `429e0ae` 添加解码器接口

**代码成果：**
- 在 `simulator.py` 中引入了可插拔的解码器接口（+217 行重构），将解码逻辑从仿真引擎中解耦。
- 为实验脚本添加了 `--decoder` 命令行参数，允许在 MWPM 和 Union-Find 之间切换。
- 设计上为后续引入 RL（强化学习）解码器预留了扩展点。

**实验结论：**
- MWPM 与 Union-Find 在标准表面码上表现接近（UF 略差，因是近似解码器），但 UF 在处理含高超边的 detector graph 时与 MWPM 一样无能为力——这一问题直到第七阶段的 qLDPC 才真正暴露。

**问题与解决：**
- 原有解码器硬编码在仿真流程中，无法对比不同解码器的效果 → 抽象为接口，支持运行时切换。

---

## 第四阶段：物理平台调研启动（2026-05-11）

**提交：** `a1c8726` 开启物理平台调研 / `f47b2eb` 调整 README 格式 / `525a312` 空文件夹问题

**代码成果：**
- 在仓库根目录创建了 `README.md`，明确了三平台（超导、中性原子、离子阱）的代表系统和主要噪声源。
- 建立了调研框架：要求从原生门集合与错误率、测量与重置时间、连接性约束限制三个维度分析各平台。
- 在 `survey/` 目录下为三个平台分别创建了占位文件。

**背景认知：**
- 确立了"表面码的逻辑错误率本质上由底层物理比特的连接性和门保真度决定"这一核心命题。
- 识别出测量与重置时间是关键瓶颈——表面码需要不断循环测量 stabilizer，测量时间过长会导致数据比特严重退相干。

**问题与解决：**
- Git 不允许上传空文件夹 → `525a312`：在每个空文件夹中放入空的 `README.md` 作为占位。
- README 格式细节需要调整 → `f47b2eb` 修正。

---

## 第五阶段：平台感知编译器 —— 关键突破（2026-05-24）

**提交：** `d86fcff` 增加了感知平台拓扑结构的中间层

**代码成果：**
- 这是项目最重要的架构升级。在原有 stim 内置电路（builtin）之上，新增了**平台感知编译器层**（Platform-Aware Compiler），共 37 个文件变更、+2296 行代码。
- 新建 `compilers/` 包，包含：
  - `base.py`（301 行）：编译器基类，定义了逐门构建电路的通用框架。
  - `superconducting.py`（132 行）：超导平台编译器，4 步定向 CZ 调度，对非参与 qubit 施加 idle 噪声。
  - `neutral_atom.py`（157 行）：中性原子双区架构（Storage ↔ Entangling zone），建模 qubit 移动的 idle 退相干。
  - `trapped_ion.py`（137 行）：离子阱 QCCD 架构，建模离子输运的 idle 退相干。
- 实验脚本升级：支持 `--use_compiler` 参数在 builtin 和 compiler 两种模式间切换。
- 结果目录重组为 `builtin/` 和 `compiler/` 两组，方便对比。
- 补充了 `test_compilers.py`（219 行）测试文件。

**实验结论 —— 这是整个项目最重要的科学发现：**

1. **builtin 模式下 PL 随 d 增大呈指数级下降**：离子阱仅需 d=9 即可达 PL ~ 10⁻⁷，中性原子 d=7 可达 10⁻⁶，完美再现了教科书的阈值定理行为。

2. **compiler 模式下 PL 几乎不随 d 改善**：d 从 3 增大到 9，三平台的 PL 基本持平（10⁻² ~ 10⁻³ 量级）。例如中性原子 compiler 模式：d=3 时 PL=5.66×10⁻³，d=9 时 PL=5.99×10⁻³——几乎没有改善。

3. **根本原因：idle 退相干 ∝ 总 qubit 数 2d²−1**。增大码距 → 更多 qubit 在移动/测量期间经历 idle → 纠错增益被 idle 噪声"吃掉"。

4. **PL 对物理错误率 p 的敏感度完全不同**：
   - builtin：p 变化 10×（0.3×→3.0×），PL 跨 3-4 个数量级。
   - compiler：p 变化 10×，PL 变化不到 1 个数量级。idle 制造了不可压缩的噪声基底。

5. **中性原子 compiler PL 在所有条件下最高**：原因是双区架构的 qubit 移动（每轮 4 批 × 来回 = 8 次全局 idle）产生了大量退相干积累。

**问题与解决：**
- 发现内置测试的实验三结果有问题，暂未修复。
- builtin 模式忽略平台物理实现细节，无法反映真实硬件约束 → compiler 层显式建模了每个平台的物理操作，揭示了 idle 噪声的主导地位。

---

## 第六阶段：平台参数修正（2026-06-08）

**提交：** `97fe97e` 修改了平台感知的编译器中的部分逻辑

**代码成果：**
- 修正了编译器中各平台的 T1/T2 参数：超导 T1=T2=100μs，中性原子 T1=1s / T2=1ms，离子阱 T1=T2 >10s。
- 更新了中性原子编译器的 idle 噪声计算逻辑，区分了 Storage zone 和 Rydberg zone 的不同退相干时间。
- 补充了 `compilers/README.md`（123 行），详细说明了各编译器的物理模型和噪声推导。
- 更新了 `RESULTS.md`，加入各平台的物理假设参数表和噪声来源引用。
- 重新运行所有 compiler 实验，更新了结果数据。

**实验结论（修正参数后重新实验）：**

1. **builtin vs compiler 的结论鸿沟被进一步确认**：
   - builtin：要达到 PL=10⁻⁶，中性原子仅需 d=7（PL=4.29×10⁻⁷），超导需要 d=15（PL=8.00×10⁻⁷），离子阱仅需 d=9（PL=6.67×10⁻⁷）。
   - compiler：即使 d=19，三平台的 PL 仍 > 2×10⁻³，距 10⁻⁶ 差约 3 个数量级。

2. **两种模式给出完全相反的工程结论**：
   - builtin 认为合理码距（d=7~15）即可实现实用化量子纠错。
   - compiler 认为即使 d=19 也无法达标，需要从物理层面（延长 T2、加速操作）而非编码层面解决问题。

**问题与解决：**
- 此前编译器对所有平台使用了统一的 T1/T2 假设，不符合物理实际 → 根据各平台代表性系统的文献数据（Google Willow、Harvard/QuEra、Quantinuum H2）分别设定了差异化的相干时间参数。

---

## 第七阶段：qLDPC 码对比研究（2026-06-08~09）

**提交：** `cbefb31` 完成 qLDPC，补充 builtin 的 exp3

**代码成果：**
- 这是项目的第二大架构扩展，新增 17 个文件、+2454 行代码。
- 新建 `qldpc/` 包：
  - `bb_code.py`（307 行）：实现了 [[72,12,6]] BB 码的构造，包括校验矩阵 H_X/H_Z、Tanner 图、逻辑算子。
  - `circuit_builder.py`（480 行）：qLDPC 电路构建器，基于 Tanner 图自动生成 syndrome extraction 电路。
  - `__init__.py`：贪心图着色算法，用于将非冲突的 check 并行化（batching），将 36 个 X-check 和 36 个 Z-check 各分约 4 批次。
- 新建 `compilers/qldpc_neutral_atom.py`（341 行）：基于与 surface code 编译器相同的双区物理模型，适配 qLDPC 的非局部连通性。
- 新增实验四（`exp4_qldpc_surface_compare.py`，253 行）：在中性原子平台上对比 qLDPC BB 码与 surface code 的逻辑错误率。
- 引入 BP+OSD 解码器（ldpc 库），解决 qLDPC 的 detector graph 含不可分解超边（hyperedge）的问题——MWPM 无法处理这种 graph。
- 补充了此前缺失的 builtin 模式实验三结果。

**实验结论：**

1. **两种码均被 idle 退相干压垮**：所有 PL 值（0.006~0.23）远高于门错误率 p（0.0005~0.01），idle 噪声是绝对主导，门错误的影响被完全淹没。

2. **qLDPC 的 shuttling 惩罚约为 2-10×**：
   - qLDPC 每轮需 8 批次 check（vs surface code 4 批次），每轮 idle 时间翻倍（1.6ms vs 0.8ms）。
   - 在 T2=1ms 的条件下，idle 退相干主导了 PL，qLDPC 因更多移动操作而更差。

3. **编码密度 vs 保真度权衡**：
   - qLDPC 用 144 个物理 qubit 编码 12 个逻辑 qubit（k=12），编码率 8.3%。
   - Surface code d=5 用 49 个物理 qubit 编码 1 个逻辑 qubit，编码率 2.0%。
   - qLDPC 以 12× 的编码密度优势换取 ~3-10× 更高的 PL。在大规模逻辑 qubit 需求场景下，这种权衡可能是有利的。

4. **Surface code d=5 的 PL > d=3 的 PL**（反常现象）：
   - d=3 PL=5.7×10⁻³，d=5 PL=6.3×10⁻³（p=0.0005 时）。
   - 原因：两者都处于 idle 主导的阈值以上区域，d=5 有更多 qubit（49 vs 17），每轮更多 qubit 经历 idle → 总逻辑错误率反而更高。

5. **BP+OSD 解码器性能**：
   - OSD-0 在低 p（≤0.001）下能有效纠错，但在高 p 下性能下降明显。
   - 解码速度是瓶颈：~2s/shot，实验四 qLDPC 部分耗时约 25 分钟（仅 50 shots/数据点）。

**问题与解决：**
- qLDPC 的 detector graph 含超边，MWPM 无法解码 → 引入 BP+OSD 解码器（OSD-0），原生支持超边。但 OSD-0 是最弱变体，解码速度慢。
- 此前实验三只有 compiler 模式结果，缺少 builtin 模式对比 → 补充运行了 builtin 模式实验三。

---

## 横贯所有实验的核心结论

| 结论 | 来源实验 | 详细说明 |
|------|---------|---------|
| **idle 错误率是 PL 的主导因素，不是门错误率** | 实验一~四 | compiler 模式下，PL 对 p 不敏感（idle 制造了噪声基底）；builtin 模式下 PL 对 p 高度敏感（无 idle 建模差异） |
| **码距增大不一定改善 PL** | 实验二、三、四 | 当 idle ∝ qubit 数时，纠错增益可能被更多 qubit 的退相干完全抵消 |
| **builtin vs compiler 给出完全相反的设计指导** | 实验一~三 | builtin：d=7~15 即可 PL=10⁻⁶；compiler：d=19 也无法达标 |
| **中性原子在 compiler 模式下 PL 最高** | 实验一~三 | 双区架构的 qubit 移动引入大量 idle（每轮 8 次全局移动），T2=1ms 是主要瓶颈 |
| **qLDPC 的高编码率优势被 shuttling 惩罚削弱** | 实验四 | 非局域连通性 → 更多移动批次数 → 更多 idle，但在大规模逻辑 qubit 场景仍有编码密度优势 |

---

## 总结

这个项目从 2026 年 4 月底到 6 月初，经历了 7 个阶段的迭代，共 13 次提交（不含 merge）：

| 阶段 | 时间 | 核心问题 | 解决方式 |
|------|------|---------|---------|
| 奠基 | 4/28 | 从零搭建仿真框架 | 基于 stim + pymatching 构建模块化架构 |
| 基线 | 5/5 | 缺少系统性实验文档 | 补写实验说明，明确 TODO 方向 |
| 解码器 | 5/10 | 解码器硬编码，无法对比 | 抽象为可插拔接口 |
| 调研 | 5/11 | 需要物理平台数据 | 建立三平台调研框架 |
| 编译器 | 5/24 | builtin 忽略物理实现细节 | 新增平台感知编译器，揭示 idle 噪声主导 |
| 参数修正 | 6/8 | T1/T2 假设不符合物理实际 | 按文献数据差异化各平台参数 |
| qLDPC | 6/9 | MWPM 无法解码 qLDPC 超边 | 引入 BP+OSD，完成码型对比 |

**最重要的科学发现**：在真实的物理约束下（平台感知编译器），idle 退相干（而非门错误率）是限制表面码逻辑错误率的主导因素。增大码距会引入更多 qubit 的 idle 退相干，可能完全抵消纠错增益。这一发现在 surface code 和 qLDPC 上均得到验证，这意味着实用化量子纠错的首要工程目标不是降低门错误率，而是**延长相干时间**和**加速物理操作**。
